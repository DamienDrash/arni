# ─────────────────────────────────────────────────────────────────────────────
# ARIIA v2.0 – Environment Configuration
# ─────────────────────────────────────────────────────────────────────────────
# Kopiere diese Datei nach .env und passe die Werte an:
#   cp .env.example .env
# ─────────────────────────────────────────────────────────────────────────────

# ── Gateway ──────────────────────────────────────────────────────────────────
ENVIRONMENT=development
LOG_LEVEL=INFO
GATEWAY_PUBLIC_URL=https://services.frigew.ski/arni
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://services.frigew.ski

# ── Database ─────────────────────────────────────────────────────────────────
POSTGRES_PASSWORD=ariia_dev_password
DATABASE_URL=postgresql+psycopg://ariia:${POSTGRES_PASSWORD}@ariia_postgres:5432/ariia

# ── Security & Auth ──────────────────────────────────────────────────────────
AUTH_SECRET=CHANGE-ME-to-a-long-random-secret-at-least-32-chars
SYSTEM_ADMIN_EMAIL=admin@ariia.io
SYSTEM_ADMIN_PASSWORD=CHANGE-ME-secure-admin-password

# ── OpenAI ───────────────────────────────────────────────────────────────────
OPENAI_API_KEY=sk-...

# ── Stripe Billing ───────────────────────────────────────────────────────────
# Diese Werte werden über das Admin-Dashboard (Plans-Seite) oder die API
# in der DB gespeichert. Alternativ können sie hier als Fallback gesetzt werden.
#
# 1. Erstelle ein Stripe-Konto: https://dashboard.stripe.com/register
# 2. Hole die API-Keys: https://dashboard.stripe.com/test/apikeys
# 3. Erstelle einen Webhook: https://dashboard.stripe.com/test/webhooks
#    Endpoint-URL: ${GATEWAY_PUBLIC_URL}/admin/billing/webhook
#    Events: checkout.session.completed, customer.subscription.updated,
#            customer.subscription.deleted, invoice.payment_succeeded,
#            invoice.paid, invoice.payment_failed
#
# STRIPE_SECRET_KEY=sk_test_...          # Nur für seed_stripe_products.py Script
# STRIPE_PUBLISHABLE_KEY=pk_test_...     # Wird im Frontend benötigt (optional)
# STRIPE_WEBHOOK_SECRET=whsec_...        # Webhook Signing Secret

# ── Docker Compose ───────────────────────────────────────────────────────────
FORWARDED_ALLOW_IPS=*
